import styles from '../css/directory-css/InsaneDeals.module.scss';

function InsaneDeals () {
    return (
        <main className={`${styles.main} wrapper`}>
        </main>
    );
}

export default InsaneDeals;