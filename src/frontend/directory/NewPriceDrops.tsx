import styles from '../css/directory-css/NewPriceDrops.module.scss';

function NewPriceDrops () {
    return (
        <main className={`${styles.main} wrapper`}>
        </main>
    );
}

export default NewPriceDrops;